    // the initial value is set empty '' so that it can be concatenated
    let otp = ''; 

    //concatenating the OTP to setFinalOTP
    for(i=0;i<otpInputs.length;i++){
      let input = otpInputs[i].current.value;
      console.log(input);
      otp += input;
    }
    console.log(otp);
    setFinalOTP(otp);
  